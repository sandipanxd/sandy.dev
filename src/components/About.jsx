export default function About() {
  return (
    <section className="split">
      <h2>About</h2>
      <p>
        Learning full stack development one stage at a time — currently building HTML/CSS/JS
        fundamentals before moving into React and a Node/Express + MongoDB backend.
      </p>
    </section>
  );
}
